services:
  - type: web
    name: inc-voting-system
    runtime: node
    plan: free
    buildCommand: npm run install-all && npm run build-ui
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
